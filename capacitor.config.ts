import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madan.customer',
  appName: 'Madan Customer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
